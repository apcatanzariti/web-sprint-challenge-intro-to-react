import React from 'react';
import styled from 'styled-components'

export default function Info ( {toggle} ) {
    return (
        <div className='active'>Working on getting the info to show when the character name clicked but ran out of time</div>
    );
}